# 실제 키가 든 .env 파일은 절대 커밋하지 않습니다. (.gitignore 로 이미 제외됨)
# 이 파일을 복사해서 .env 로 이름을 바꾸고, 실제 발급받은 키를 넣어서 사용하세요.
#   cp .env.example .env      (Git Bash / macOS / Linux)
#   copy .env.example .env    (Windows cmd)

OPENAI_API_KEY=sk-여기에-발급받은-키-붙여넣기
